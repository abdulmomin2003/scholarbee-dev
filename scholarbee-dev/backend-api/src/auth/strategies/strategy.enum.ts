export enum AuthStrategyEnum {
  LoginStrategy = 'login',
  RefreshStrategy = 'refresh',
  ResourceProtectionStrategy = 'resource-protection',
  OptionalAuthStrategy = 'optional-auth',
  GoogleStrategy = 'google',
}